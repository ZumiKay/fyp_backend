_383‍.x([["validate_registerInput",()=>validate_registerInput]]);const { body, validationResult } = require('express-validator');

       const validate_registerInput = [
        body('fullname').isString().notEmpty(),
        body('studentID').isNumeric().notEmpty(),
        body('department').isString().notEmpty(),
        body('phone_number').isNumeric().notEmpty(),
        body('email').isEmail().notEmpty(),
        (req, res ,next) => {
            const errors = validationResult(req)
            if(!errors.isEmpty()) {
                return res.status(400).json({erros: errors.array()})
            }
            next()
        }
    ]
